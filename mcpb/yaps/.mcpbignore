# Extra pack-time excludes on top of mcpb's defaults
# (defaults already cover .git, .DS_Store, *.log, package-lock.json, *.map,
# node_modules/.cache, node_modules/.bin).
*.mcpb
.mcpbignore
node_modules/.package-lock.json
